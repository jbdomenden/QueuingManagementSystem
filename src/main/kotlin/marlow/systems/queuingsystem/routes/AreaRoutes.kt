package marlow.systems.queuingsystem.routes

import io.ktor.server.routing.Route
import io.ktor.server.routing.route

fun Route.areaRoutes() {
    route("/areas") {
        // skeleton kept for create, update, and list by department using same controller style
    }
}
